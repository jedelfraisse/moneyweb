@page "/account"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inject NavigationManager Navigation

@* The account page merged into the dashboard ("/") as a side column — keep this route alive so old
   links/bookmarks still land somewhere instead of 404ing. *@
@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/", replace: true);
}
